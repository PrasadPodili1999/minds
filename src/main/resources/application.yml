server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mindtreeminds
    username: root
    password: 2587
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate.ddl-auto: update
    show-sql: true
    properties.hibernate.format_sql: true